# Plagiarism-detector-java
Java Swing–based plagiarism detection desktop app that compares two code/text files using a token-frequency hashing algorithm, highlights matching content, generates PDF reports, and stores analysis history in MySQL via JDBC with a modular UI-logic-DB architecture.
